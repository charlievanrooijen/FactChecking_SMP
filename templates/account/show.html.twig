{% extends 'base.html.twig' %}

{% block title %}Account{% endblock %}

{% block body %}
    <h1>Account</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ account.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ account.name }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ account.Password }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-light my-3" href="{{ path('app_account_index') }}">back to list</a>
    </br>
    <a class="btn btn-light mb-3" href="{{ path('app_account_edit', {'id': account.id}) }}">edit</a>

    {{ include('account/_delete_form.html.twig') }}
{% endblock %}
